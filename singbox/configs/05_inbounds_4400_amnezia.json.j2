{
	{# watashi v12.2.130u: amnezia used to be described twice, once as an "awg"
	   endpoint of this core and once as a daemon of its own. sing-box has no
	   awg endpoint at all - official 1.14.0 answers "unknown endpoint type:
	   awg" and then refuses every other protocol with it - so that half was
	   never anything but a dead switch. AmneziaWG is served by awg-quick on
	   the interface named in other/amnezia/awg_utils.sh, and amnezia_enable
	   is the one switch that turns it on. This file is left in place, and
	   empty, so an older installation that still has it on disk is simply
	   overwritten with nothing instead of keeping the old endpoint. #}
}
